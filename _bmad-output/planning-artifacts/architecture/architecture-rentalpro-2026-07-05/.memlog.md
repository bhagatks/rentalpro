---
scope: RentalPro.ai full-platform MVP
purpose: build substrate
altitude: initiative→features
updated: 2026-07-05T18:39
---

- (constraint) Inherited from SPEC (locked): multi-tenancy Day 1 w/ organizationId+RLS; Texas-only; double-entry tax-ready ledger; immutable audit trail; FHA/FCRA by design; non-bypassable governance rails; chat/SMS/email only; Seam+Stripe+Plaid partners; subdomain branding
- (constraint) Inherited from CAP-11 (locked): Clerk auth w/ Organizations; single Postgres DB + RLS; app.current_org_id session var; subdomain tenant resolution; hybrid org provisioning
- (decision) User decision (chat 2026-07-05): mobile is Phase 2 UI but architecture is API-first multi-client from Day 1 — mobile extracted from web, built in parallel later; backend/business logic must not be web-coupled
- (decision) Monorepo modular monolith on create-t3-turbo pattern (Turborepo+pnpm): apps/web (Next.js), apps/mobile (Expo, Phase 2), packages/{api,core,db,agents,integrations,ui}. Alternative weighed: separate Express/Hono API service — rejected for MVP (ops burden, no benefit at this scale, Vercel+Supabase already spec direction); revisit at AWS/GCP migration
- (decision) API contract: tRPC v11 routers in packages/api consumed by both Next.js and Expo (t3-turbo pattern) — end-to-end types, no OpenAPI codegen drift. Public REST facade deferred to Phase 2 (M9 open API). Webhooks are the only non-tRPC HTTP surface
- (decision) ORM: Drizzle over Prisma — Drizzle ships first-class Postgres RLS (crudPolicy, Supabase role helpers); Prisma has none and RLS is Constraint #1. Supersedes Prisma references in docs/rules/multi-tenant.md and HANDOFF (both predate this run). Verified via web 2026-07
- (decision) Agent orchestration: Inngest durable step functions — event-driven fit (rent-late, video-uploaded events fan out), multi-tenant concurrency keys, serverless-native on Vercel, waitForEvent for human-approval pauses. Alternative: Trigger.dev (long-running compute, self-hostable) — revisit if agent steps outgrow serverless timeouts [ASSUMPTION]
- (decision) LLM layer: Vercel AI SDK 6 (ToolLoopAgent, MCP support) with Anthropic claude-sonnet-5 default for agent reasoning, claude-haiku-4-5 for cheap classification (triage, categorization) [ASSUMPTION on model split]
- (version) Verified current 2026-07: Next.js 16.2.x LTS; Expo SDK 56 (RN 0.85, React 19.2, New Architecture mandatory); tRPC v11; @clerk/expo Core 3 (orgs on Expo); Drizzle RLS GA; AI SDK 6; Tailwind v4 + NativeWind v5; Postgres 17 (Supabase)
- (decision) Comms adapters: Twilio (SMS), Resend (email), in-app chat native [ASSUMPTION — commodity choices, swap-safe behind ports]
- (decision) Money as integer cents (bigint), timestamps timestamptz UTC, IDs uuidv7 — supersedes cuid suggestion in docs/rules/multi-tenant.md; uuidv7 is index-friendly and DB-generatable [ASSUMPTION]
- (question) E-sign provider (DocuSign/Dropbox Sign/Documenso), screening API vendor, Plaid-vs-Stripe-only bank feeds — deferred to epic-level; ports defined now
- (event) Reviewer gate ran: lint + 3 parallel lenses (rubric, version fact-check, adversarial). Full reviews in reviews/. Verdicts: versions PASS w/ 2 corrections; rubric APPROVE W/ REVISIONS; adversarial NOT READY (12 seams, 3 critical)
- (decision) Gate fixes applied: new AD-12 entity ownership registry (Lease/LeaseTerms via getEffectiveTerms; derived statuses single-computer), AD-13 approval protocol (workflow sole executor, single-transition ApprovalRequest), AD-14 typed event catalog + envelope (packages/events), AD-15 comms choke point (adapters export-scoped to core/comms; template approval gate), AD-16 storage convention (org-prefixed private buckets, file rows, signed URLs only), AD-17 ops alarming (Sentry, stalled-run + security alerts, Supabase PITR + restore test)
- (decision) Gate tightenings: AD-7 source_ref idempotency + posting catalog (kills double posting); AD-4 per-aggregate concurrency + guard+mutate in one txn; AD-3 no client data channel bypasses packages/api + publicProcedure pattern + exact sanctioned-surface list; AD-5 enabled vs autonomyMode split; AD-6 core/trace intent+result events; AD-8 statutory ordering as rules-engine preconditions; AD-2 non-superuser role, service-role key banned in app code; AD-11 core/time legal calendar w/ per-rule day semantics
- (event) Erratum applied to docs/DELINQUENCY-RULES-ENGINE.md step 6: trace commits atomically with ledger post, not after notify (AD-6 conflict found by adversarial review F-9)
- (assumption) NativeWind v5 still pre-release 2026-07: adopt only if GA by Phase 2, else v4 + Tailwind-3 preset scoped to mobile. Clerk expo peer >=53 <57 — re-verify at Phase 2. Sentry as error tracker. Screening PII stays vendor-side (decision + report ref only)
- (event) spine finalized
